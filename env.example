# LANStreamer Environment Configuration
# Copy from env.example and modify as needed

# Server Configuration
NODE_ENV=development
PORT=3001
HOST=0.0.0.0

# Security (CHANGE THESE IN PRODUCTION!)
# JWT secret for authentication tokens - use a long, random string
# Example: JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random-123456789
JWT_SECRET=lanstreamer-dev-secret-key-change-in-production
JWT_EXPIRES_IN=24h

# Admin login credentials (CHANGE THESE!)
# Username for admin dashboard access
ADMIN_USERNAME=admin
# Password for admin dashboard access - use a strong password
ADMIN_PASSWORD=lanstreamer123

# Session secret for additional security - use a long, random string
# Example: SESSION_SECRET=your-super-secret-session-key-here-make-it-long-and-random-987654321
SESSION_SECRET=session-secret-change-this

# FFmpeg Configuration
FFMPEG_PATH=ffmpeg
FFMPEG_LOG_LEVEL=info
MAX_CONCURRENT_STREAMS=16

# Icecast Configuration
ICECAST_HOST=localhost
ICECAST_PORT=8000
ICECAST_ADMIN_PASSWORD=hackme
ICECAST_SOURCE_PASSWORD=hackme

# Icecast Path Configuration (optional - LANStreamer will auto-detect if not specified)
# Use these to override automatic detection with custom installation paths
# ICECAST_EXE_PATH=C:\Program Files (x86)\Icecast\bin\icecast.exe
# ICECAST_CONFIG_PATH=C:\Program Files (x86)\Icecast\icecast.xml
# ICECAST_ACCESS_LOG=C:\Program Files (x86)\Icecast\logs\access.log
# ICECAST_ERROR_LOG=C:\Program Files (x86)\Icecast\logs\error.log
# ICECAST_CUSTOM_PATH=C:\Program Files (x86)\Icecast

# Audio Configuration
DEFAULT_BITRATE=128k
DEFAULT_SAMPLE_RATE=44100
DEFAULT_CHANNELS=2
AUDIO_BUFFER_SIZE=1024

# Network Configuration
STREAM_BASE_URL=http://localhost:8000
CLIENT_PORT=8080
WEBSOCKET_PORT=3001

# Logging
LOG_LEVEL=info
LOG_FILE_PATH=./logs
LOG_MAX_SIZE=10m
LOG_MAX_FILES=5

# Development
ENABLE_CORS=true
ENABLE_RATE_LIMITING=true

# Database (file-based storage)
CONFIG_DB_PATH=./data/config.json
STREAMS_DB_PATH=./data/streams.json

# File Upload
MAX_FILE_SIZE=10mb
UPLOAD_PATH=./uploads
